# wip — package-iteration-radius
# Intended branch: codex/package-iteration-radius
# Started: 2026-08-24

#:layer #!pragmatics
@dialect:Spw.p

^["roots"]{
 @cluster: ~".agents/plans/plan-ecology-clustering/plan-ecology-clustering.spw"
 @plan: ~".agents/plans/package-iteration-radius/PLAN.md"
 @artifact: ~".agents/plans/package-iteration-radius/package-iteration-radius.spw"
 @root_manifest: ~"package.json"
 @lockfile: ~"package-lock.json"
 @doctor: ~"packages/spw-cli/src/doctor.ts"
 @bench: ~".agents/plans/cli-benchmarking-infra/cli-benchmarking-infra.spw"
 @runtime_dx: ~".agents/plans/runtime-dx-foundation/runtime-dx-foundation.spw"
 @install: ~".agents/plans/spw-site-install/PLAN.md"
 @gap_affinity: ~".agents/plans/gap-affinity-tooling/gap-affinity-tooling.spw"
}

^["intent"]{
 ~#goal: "Make package boundaries and machine-specific build assumptions truthful, then reduce the build, test, dependency, and conceptual radius of ordinary changes."
 ~#taste: "fast first trust, causal diagnostics, compact onboarding, portable consumers, and no hidden globals"
}

^["development"]{
 lane: #execution_truth
 ladder: #service
 develops: [#package_hygiene, #developer_experience, #performance_discipline, #failure_locality, #documentation]
 review_surfaces: [@plan, @artifact, @root_manifest, @lockfile, @doctor, @bench, @runtime_dx, @install]
 snippet_routes: [#package_contract, #environment_receipt, #iteration_radius, #failure_code, #consumer_smoke]
}

^["concept_index"]{
 primary: [#iteration_radius, #package_contract, #environment_receipt, #dependency_cone, #first_actionable_result]
 surfaces: [#seed, #runtime, #lsp, #cli, #extension, #consumer, #ci]
 tensions: [#root_convenience_vs_leaf_truth, #hoisting, #native_portability, #cache_ambiguity, #version_lockstep]
 transfers: [#language, #service, #editor, #consumer]
}

^["recursive_improvement"]{
 loop: [
  "? audit declared packages, imports, exports, tools, and supported environments",
  "< run the smallest truthful leaf task and one clean consumer smoke",
  "$ measure dependency cone, invalidation, first actionable result, completion, and environment identity",
  "^ revise one manifest, diagnostic, script, or guide with a migration receipt",
  "% preserve public product identity, integration verification, redaction, and cross-machine comparability",
 ]
 gates: [#audit_first, #clean_consumer, #environment_compatible, #leaf_integration_parity, #redaction]
}

^["stream"]{
 >>[2026-08-24 11:18] observe — the npm workspace has a lockfile and root Node range, while internal package manifests are private lockstep shells and most dependency, export, toolchain, and task truth remains centralized at the root.
 >>[2026-08-24 11:19] observe — the editor workspace declares its own compiler and bundler ranges, and current doctor/pack foundations do not yet emit one comparable environment receipt.
 >>[2026-08-24 11:20] define — iteration radius is the dependency, build, test, cache, and conceptual surface a change forces a developer to traverse before receiving a trustworthy result.
 >>[2026-08-24 11:21] decide — distinguish source, manifest, lockfile, runtime, native-addon, ABI, platform, cache, and external-tool failures before attributing a problem to language or application code.
 >>[2026-08-24 11:22] decide — ordinary feature work should have a truthful leaf path plus a root integration path; reducing radius may not skip the public export or generated-consumer product.
 >>[2026-08-24 11:23] taste — onboarding convenience is mature when it supplies a short successful path, an explanation of inherited decisions, and an explicit route for migration or escape.
 >>[2026-08-24 11:33] observe — one identity-free consumer snapshot describes the same recursion dependency as a mandatory sibling checkout in one active guide and as a pure pinned git dependency in another; setup truth needs one checked prerequisite graph.
 >>[2026-08-24 11:34] observe — the same consumer records multi-minute native proof-runtime startup and broad generated-code invalidation for small tasks, making dependency cones and first actionable result more useful than total build time alone.
 >>[2026-08-24 11:35] decide — generate or validate human, machine-facing, and CLI setup instructions against the environment contract; documentation disagreement is a build failure with the conflicting fields named.
 >>[2026-08-24 11:50] observe — the gap-affinity composite build exposed that spw-cli imports spw-runtime while its TypeScript project referenced only spw-seed, causing runtime source to enter the CLI root instead of resolving as a package dependency.
 >>[2026-08-24 11:51] decide — TypeScript project references are part of package truth; the CLI now declares its runtime reference and the seed→runtime→CLI composite build completes from the leaf package graph.
}

^["open"]{
 ?[publication]: "Which packages are intended to become independently consumable first, and which should remain private implementation boundaries?"
 ?[toolchain_ranges]: "Should extension compiler/bundler ranges converge with root tools or remain a tested compatibility matrix?"
 ?[task_graph]: "Can npm workspace scripts meet the measured radius target before a task-graph or cache framework is justified?"
}

^["commits"]{
 ~[1]: ".[plans] — define package contracts, environment receipts, and iteration-radius measures"
 ~[2]: "#[packages] — audit declared and imported workspace boundaries"
 ~[3]: "vocab[tooling] — add redacted environment and failure-locality receipts"
 ~[4]: "&[doctor] — explain runtime, lockfile, ABI, native, platform, and cache mismatches"
 ~[5]: "&[packages] — add truthful leaf verification and package export smoke paths"
 ~[6]: "#[bench] — measure dependency cones, invalidation, and first actionable feedback"
 ~[7]: "![packages] — verify clean install, pack, import, CLI, LSP, and extension matrices"
 ~[8]: ".[docs] — publish onboarding, package reference, and migration decisions"
}

^["cache"]{
 ~#status: "active"
 ~#base_ref: "main@c46558ffbb29a10abb1fee11dbec8aa522cbc8b3"
 ~#rebased_at: "2026-08-24"
 ~#hygiene_split: "one bounded cross-plan correction in gap-affinity-tooling; the broader manifest/environment audit remains in codex/package-iteration-radius"
 ~#files_hot: "packages/spw-cli/tsconfig.json, .agents/plans/package-iteration-radius/PLAN.md, .agents/plans/package-iteration-radius/wip.spw, .agents/plans/package-iteration-radius/package-iteration-radius.spw"
 ~#next_commit: "2"
 ~#open_count: "3"
 ~#last_stream: "2026-08-24 11:51"
 ~#taste_debt: "Public package intent, extension toolchain convergence, and the threshold for build orchestration remain unresolved until audit and radius evidence exist."
}

^["done"]{
}

#>wonder_package_iteration_radius_1
?["How far must a small change travel before it becomes trustworthy?"]{
 ~#answer_shape: [#changed_package, #dependency_cone, #invalidated_tasks, #first_actionable_ms, #integration_ms]
 !probe{ "change one fixture in a leaf package, then compare the smallest truthful task with the root integration path without skipping product identity." }
}

#>wonder_package_iteration_radius_2
?["Is this a source failure or a machine agreement failure?"]{
 ~#answer_shape: [#failure_class, #environment_delta, #next_action, #redacted_receipt]
 !probe{ "hold source and lockfile constant across two environment receipts; classify ABI, platform, native, and cache deltas before rerunning semantic tests." }
}
