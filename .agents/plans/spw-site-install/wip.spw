# wip — spw-site-install
# Branch: feature/spw-site-install
# Started: 2026-03-27

#:layer #!pragmatics

^["roots"]{
 @cluster: ~".agents/plans/plan-ecology-clustering/plan-ecology-clustering.spw"
 @plan: ~".agents/plans/spw-site-install/PLAN.md"
 @artifact: ~".agents/plans/spw-site-install/spw-site-install.spw"
 @workspace: ~".spw/workspace.spw"
 @release: ~".agents/plans/_archive/v030-release-prep/v030-release-prep.spw"
}

^["intent"]{
 ~#goal: "Define the mounted-workbench model for arbitrary consumer repositories: consumers own .spw canon and review evidence; .spw/_workbench supplies versioned language and review infrastructure."
 ~#taste: "clarity, layering, portability — consumer authority remains distinct from mounted instrumentation"
}

^["development"]{
 lane: #execution_truth
 ladder: #service
 develops: [#mount_protocol, #site_independence, #install_truth, #exchange_model, #repo_local_review]
 review_surfaces: [@cluster, @plan, @artifact, @workspace, @release]
 snippet_routes: [#mount_example, #doctor_output, #submodule_note, #site_install_copy]
}

^["concept_index"]{
 primary: [#submodule, #mount, #install, #site, #engagement, #exchange, #review]
 surfaces: [#cli, #runtime, #editor, #docs, #sites, #agents]
 tensions: [#independence, #convenience, #resolution, #public_shape, #authority_leakage]
 transfers: [#service, #release]
}

^["recursive_improvement"]{
 loop: [
  "? reread site/install boundaries before adding more convenience layers",
  "< ship one mount, init, or doctor behavior that makes site ownership clearer",
  "$ record what became truer for a consumer repository entering the ecology",
  "^ feed the stable boundary language into CLI, docs, editor, and release surfaces",
  "% keep convenience claims narrower than the submodule protocol they depend on",
 ]
 gates: [#site_ownership, #mount_truth, #install_clarity]
}

^["parallel_work"]{
 #absorb_spwq_cli: "CLI verb taxonomy must stabilize before site init can teach command names"
 #v030_release_prep: "release tag waits on site-install mounting being real"
 #ecosystem_surface_governance: "installable gate definition flows from governance registry into mount.spw"
 shared_hot_files: ["packages/spw-runtime/src/site-install.ts", "packages/spw-runtime/src/index.ts", "packages/spw-lsp/src/helpers.ts", "packages/spw-lsp/src/server-index.ts", "packages/spw-cli/src/init.ts", "packages/spw-cli/src/doctor.ts", "packages/spw-cli/templates/init/base/.spw/mount.spw", ".spw/conventions/submodule.spw"]
}

^["supporting_documents"]{
 @git_submodule_docs: "https://git-scm.com/book/en/v2/Git-Tools-Submodules"
 @mount_protocol: ~".spw/conventions/submodule.spw"
 @cli_conventions: ~".spw/conventions/cli.spw"
 @engagement_model: ~".agents/plans/ecosystem-surface-governance/ecosystem-surface-governance.spw"
}

^["interaction_depth"]{
 question_surfaces: [#open]
 decision_log: [#stream]
 taste_checkpoints: ["2026-03-27 09:03", "2026-03-27 13:21"]
 surprise_budget: "submodule resolution paths may diverge between git clone --recursive and manual submodule add"
 resolution_pattern: "each open question resolves by shipping a commit that makes the answer observable in consumer-local behavior"
}

^["stream"]{
 >>[2026-03-27 09:00] observe — prior approach assumed npm packages as primary distribution. User decided overnight that submodule model better preserves repository independence and engagement/exchange semantics.
 >>[2026-03-27 09:01] decide — workbench mounts at .spw/_workbench. The _ prefix signals infrastructure-not-content. Site's .spw/ surfaces are its own canon; workbench provides parser, runtime, LSP, CLI, and spec library underneath.
 >>[2026-03-27 09:02] decide — mount.spw is the engagement contract: it declares which workbench version the site tracks, which spec subsystems it engages with, and how resolution paths flow from site content through the engine.
 >>[2026-03-27 09:03] taste — this model is more honest than npm publish for early ecosystem: the relationship between repos is visible (git submodule), versionable (pin to commit), and reversible (remove submodule). npm can come later as a convenience layer once the protocol stabilizes.
 >>[2026-03-27 09:04] observe — independent consumers may already contain code and local conventions; each needs its own .spw authority rather than a copied workbench identity.
 >>[2026-03-27 09:05] decide — engagement is selective: a site doesn't adopt all 11 spec subsystems at once. mount.spw lists which subsystems are active. Exchange is contributory: a site can register new dialects or conventions that flow back through the submodule relationship.
 >>[2026-03-27 13:20] decide — treat this branch as the ecological anchor. Once commits 1-3 land, CLI init, CLI resolution, editor detection, health checks, docs, and governance mapping can proceed in parallel because they all share the same `_workbench` boundary.
 >>[2026-03-27 13:21] taste — ecological invigoration is not "more packages"; it is more independent sites entering the field with a clear mount contract, selective engagement, and a reversible relationship to the workbench.
 >>[2026-03-28 17:29] revise — the plan cache was stale after the site-install artifact, submodule convention, release-track truth, and portable site-init/doctor surfaces landed on main. The branch is no longer at commit 1, and install tooling already exists in CLI surfaces.
 >>[2026-03-28 17:29] observe — the remaining gap is shared mount resolution. `.spw/mount.spw` exists as seeded site metadata, but runtime and LSP still assume workbench-root defaults rather than deriving spec/CLI/LSP paths from the mounted `_workbench` contract.
 >>[2026-03-28 17:29] decide — treat commits 3 and 4 as one working arc: formalize mount.spw with tracked version + resolution paths, then export a runtime resolver that LSP and doctor can consume while keeping `.spw/_workbench` out of site-authored workspace scans.
 >>[2026-07-20 14:31] observe — mounted resolution landed in 45c6b7f3, but the plan still reported commit 3 as next and did not describe the repository-local model review that motivates mounting the workbench in developing consumers.
 >>[2026-07-20 14:32] decide — a repo-local model reads consumer-owned `.spw/` as authority, consults `.spw/_workbench` for protocols and instruments, and writes review artifacts back to consumer-owned surfaces with the mounted workbench revision recorded.
 >>[2026-07-20 14:33] decide — exchange is human-gated: consumers may contribute distilled claims, generated fixtures, or protocol improvements upstream, but the workbench does not automatically ingest consumer canon or review output.
 >>[2026-07-20 14:34] revise — use generated identity-free consumer fixtures so portability evidence never depends on a specific repository, domain, or predicted deployment shape.
 >>[2026-07-20 15:12] revise — add CLI observability as the next execution arc: root discovery, doctor, capability snapshots, and review orchestration must precede editor feature audits.
 >>[2026-08-24 10:14] observe — a scalar release number cannot tell a mounted consumer whether its language edition, syntax profile, AST/product schemas, runtime protocol, CLI envelope, and editor projection are mutually compatible.
 >>[2026-08-24 10:15] decide — mount, capabilities, and doctor disclose a compatibility vector with both revisions and per-capability status; `v0.4` remains a possible language gate, not a claim that all dimensions moved together.
 >>[2026-08-24 10:16] decide — resolved [review-entrypoint]: the mounted review skill owns authority/provenance procedure while CLI roots, doctor, capabilities, and probes remain composable instruments; no monolithic command is required.
 >>[2026-08-24 10:17] decide — resolved [review-retention]: generated caches are ephemeral, explicit consumer claims are durable by local policy, and upstream exchange is always a deliberate human-gated distillation.
}

^["open"]{
 ?[node-resolution]: "How do consumer-local node scripts resolve @spwashi/* imports — via package.json workspace paths pointing into .spw/_workbench/packages/, or via tsconfig paths?"
 ?[submodule-update]: "What is the cadence for submodule updates — pin to tagged releases, track main, or manual advance?"
 ?[multi-consumer]: "When multiple deployments share one codebase, which repository boundary owns .spw authority and review retention?"
 # resolved: [review-entrypoint] — see stream 2026-08-24 10:16
 # resolved: [review-retention] — see stream 2026-08-24 10:17
}

^["commits"]{
 ~[1]: "#[submodule] — define .spw/_workbench submodule convention and _-prefix semantics"
 ~[2]: ".[conventions] — add .spw/conventions/submodule.spw documenting the engagement model"
 ~[3]: "#[mount] — formalize mount.spw protocol: version tracking, surface engagement, resolution paths"
 ~[4]: "&[mount] — implement mount resolution in spw-runtime: read mount.spw, resolve spec paths through _workbench"
 ~[5]: "![mount] — verify mount resolution finds spec library, parser, and runtime from submodule path"
 ~[6]: "&[cli-init] — implement spw init: scaffold .spw/ with mount, index, workspace"
 ~[7]: "&[templates] — create mount.spw template with submodule connection and version pin"
 ~[8]: "&[templates] — create index.spw and workspace.spw templates for consumer-local canon"
 ~[9]: "&[templates] — create CLAUDE.md fragment for .spw-enabled consumer repositories"
 ~[10]: "![cli-init] — verify spw init creates correct .spw/ structure alongside existing _workbench submodule"
 ~[11]: "&[cli] — add submodule-aware path resolution: spw commands find packages via .spw/_workbench/"
 ~[12]: "![cli] — verify spw commands work when invoked from a consumer repository with _workbench mounted"
 ~[13]: "&[vscode] — add .spw/_workbench detection for LSP server resolution"
 ~[14]: "![vscode] — verify extension activates correctly in a consumer repository with only .spw/_workbench"
 ~[15]: "&[cli-doctor] — implement spw doctor: verify submodule present, parser loadable, LSP reachable"
 ~[16]: "![cli-doctor] — verify spw doctor output is actionable for each failure mode"
 ~[17]: "#[engagement] — define engagement protocol: how a site selects spec subsystems and conventions"
 ~[18]: "#[exchange] — define exchange protocol: how a site contributes surfaces back to the ecosystem"
 ~[19]: ".[docs] — write site-install guide: submodule setup, init, mount, editor, health check"
 ~[20]: ".[docs] — write mount protocol reference"
 ~[21]: ".[plans] — write spw-site-install.spw artifact formalizing the submodule integration model"
 ~[22]: "#[site-review] — define mounted-workbench discovery, authority, provenance, and output contract"
 ~[23]: "&[site-review] — expose a portable review entrypoint to repo-local models"
 ~[24]: "![consumer-review] — verify identity-free reviews remain consumer-owned and revision-aware"
 ~[25]: "&[cli,consumer] — expose roots, doctor, capabilities, and review orchestration contracts"
 ~[26]: "![cli,consumer] — verify relative-path output and distinct failure states in generated fixtures"
}

^["cache"]{
 ~#status: "active"
 ~#base_ref: "main@66ed9a2ea2c490522eed38004f6848c7966efc26"
 ~#rebased_at: "2026-08-24"
 ~#hygiene_split: "none"
 ~#files_hot: "packages/spw-runtime/src/site-install.ts, packages/spw-runtime/src/index.ts, packages/spw-lsp/src/helpers.ts, packages/spw-lsp/src/server-index.ts, packages/spw-cli/src/init.ts, packages/spw-cli/src/doctor.ts, packages/spw-cli/templates/init/base/.spw/mount.spw, .agents/plans/spw-site-install/PLAN.md, .agents/plans/spw-site-install/wip.spw"
 ~#next_commit: "25"
 ~#open_count: "3"
 ~#last_stream: "2026-08-24 10:17"
 ~#taste_debt: "Mount metadata still needs a tested compatibility-vector schema and doctor currently checks less than its convention promises. Keep review output consumer-owned, revision-aware, and human-gated before exchange; `_workbench` must never become an excuse to absorb consumer canon."
}

^["done"]{
 # Written at merge time.
 # ~#shipped: []
 # ~#dropped: []
 # ~#surprised: ""
 # ~#duration: ""
 # ~#taste: ""
}
