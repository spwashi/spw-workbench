# Plan: canon-rewrite-v2

Rebuild unpublished workbench progress as an operationally honest tip: field coordinates, quanta/cache identity, JS tree hygiene, corpus normalization, structural change reports, and pattern/search handles—**interleaved**, not history archaeology.

## Goal

Land a publishable `main` (or promote branch) rooted at `upstream/main@d9da60ad` whose **tree** carries the capability of the current tip (plus dirty cite/session work where sound) while **design hygiene** is restored: one noun per clock, product identity ≠ retention ≠ projection, dialect as subject and channel as wall, corpus particles/indexes navigable by CLI, packages readable as membranes.

**Taste:** execution truth over draft commit theater; coordinates over metaphors; collate before discharge; corpus structure densifies statics the same week code learns those statics—not a docs sprint after the fact.

**Commit count is not a success metric.** Ship concept-sized discharges when green; squash or split freely. The program below is ordered by **dependency and interleaving**, not by a fixed N.

---

## Scope

### In scope

- **History posture:** unpushed 31-commit log and mid-rebase `wt-canon-rewrite-v2` are disposable draft. Tip tree + design lessons are the asset.
- **Operational field doctrine:** identity / retention / projection; charge crawl; fixity/geometry statics; status vocabulary (implemented | measured | proposed | interpretive).
- **Quanta & cache:** IrRef product keys; request epoch vs session beat; BeatCache as retention only; eval vs inspect planes; granules/appositions as invalidation boundaries (proportional where possible).
- **JS architecture hygiene:** dual `src/` vs `packages/` collapse path; god-file freezes/splits; CLI folders by cost group; seed statics/subpaths; package DAG clarity.
- **Corpus normalization (interleaved):** particles, folder/index contracts, alias refs, wonder circuits, dialect defaults—first on `.spw`, `docs/theory`, `prompts`.
- **Change representation:** lex + AST compare products + Spw dual-read; pulse/cycle consume them; layout-only claims require brace + structural evidence.
- **Patterns & search:** capture/apply across syntax/pragmatics; exact `spwq` + fuzzy resonance under versioned tuning; index handles (label, fixity, expression, pathref, particle, `@bc`).
- **Diff legend (interpretive profile first):** `_` AT, `&` WITH/subject, `^` INTO/integrate; `~` potention, `*` value collapse; `#` keep vibration/resonate/particles (not rebind as free “electrical charge”).
- **CLI / info-sci naming:** math + materials navigation; sense/read/shape/effect cost claims; optional soft aliases (`grain`, etc.).
- **TypeScript:** brands, closed id unions, `satisfies` exhaustiveness; TS7 as faster engine when ready—not a redesign blocker.
- **Push:** first ff to `upstream/main` when tip is lawful and green.

### Out of scope (explicit defer)

- Faithful interactive rewrite of all 31 original SHAs.
- Full incremental lexer resume / complete tree-edit-distance AST diff (v1 is path-match + lex LCS).
- Disk CAS, multi-user OT/CRDT, precipitation engine as automatic authoring.
- Resonance-as-cache-equivalence; request traffic as product identity.
- Rebinding core `#` runtime/particle meaning to “electrical charge.”
- Full `.agents/plans` corpus rewrite in the first land (plans ecology later).
- Merging CLI and LSP packages.

---

## Doctrine (non-negotiable)

### Field coordinates (orthogonal)

| Coordinate | May enter product identity? |
|------------|----------------------------|
| contentHash, uri, span, revision | yes |
| dialect, lex profile, producer schema, boundary contract | yes |
| channel | yes for eval/discharge products; wall for who may run |
| session beat, retention tier, resonance rank, request epoch, soft `@bc:tag` | **no** |

### Clocks

- **Request epoch** — LSP handled JSON-RPC count (observe only).
- **Session beat** — runtime retention / substrate tick.
- **Format pulse** — rule-level format identity.
- Never share names across clocks.

### Collate vs discharge

- Search, census, plan-only pulse, pattern capture, dry-run = **collate**.
- Workspace write, commit, precipitate = **discharge** under channel effect ceiling.

### Status vocabulary

Every semantic claim on theory/corpus/code comments: implemented | measured | proposed | interpretive.

---

## Bootstrap (before feature work)

1. **Backup:** branch/tag tip `8bb423c7` (e.g. `backup/pre-rewrite-v2-tip`) and stash or park main dirty (cite + hot-session) on `wip/cite-tag-session`.
2. **Abort** interactive rebase on `wt-canon-rewrite-v2` (do not finish 26 edits). Export activity-hygiene lessons from rewritten stops as notes/patch if useful; do not require replaying them as history.
3. **Branch** `feature/canon-rewrite-v2` (or land on rebuild branch) from `upstream/main@d9da60ad`.
4. **Import tip capability** as a working tree (merge/checkout tip tree, or patch series)—then **re-cut** by phase laws rather than preserving draft commits.
5. Park v1 worktree `wt-canon-rewrite` as non-goal; retrospective worktree independent.

---

## Program: interleaved phases

Each phase has **code track** + **corpus track**. Finish a phase when its acceptance holds; interleave within a phase (corpus pass after the code that benefits from stable headers/handles). Do not batch all corpus work at the end.

### Phase 0 — Doctrine & clocks

**Code**

- Publish short doctrine surface (theory or `.spw/patterns`) stating coordinates, clocks, collate/discharge, identity formula.
- LSP: request-epoch naming; remove public temperature product and request-modulus material tick (`$spw/beatTick` as transition claim).
- Brand types: `ContentHash`, `RequestEpoch`, `SessionBeat` (no cross-assignment).
- Document BeatCache tier = retention only.

**Corpus**

- Link doctrine from `.spw/index` or workspace relationships.
- Status tags on brace-charge-crawl / fixity theory if touched (`proposed` → measured only when code lands).

**Acceptance:** no public API teaches request traffic as substrate pulse or product identity.

---

### Phase 1 — Address algebra (IrRef) & package freeze

**Code**

- IrRef (+ producer schema) is the only durable product address; `irRefKey` for maps.
- Law tests: dialect/schema change ⇒ new key; session beat alone ⇒ same key.
- H0 hygiene: no new code under `src/seed` or `src/runtime`; no net growth of files already >600 lines without split note.
- Declare workspace package deps (DAG machine-readable) when touching package.json.

**Corpus**

- Prefer `@alias` in new/edited refs; no new god-links into `workspace.spw` without alias.

**Acceptance:** one key builder; craft freeze documented.

---

### Phase 2 — Seed quanta, fixity statics, particle/apposition scans

**Code**

- Product kinds (v1): source snapshot, token window (+ boundary contract), parse-free apposition scan, whole-file parse product as fallback.
- Populate fixity / actPlacement where parser knows; phrase census as measured static.
- Closed unions where cheap: `LexProfileId`, resonance scheme ids (kill open `string` creep).
- Single **prepare** path for dialect detect + preprocess shared by runtime/CLI/LSP callers (converge over phase, not one mega-PR).

**Corpus (norm pass A — particles)**

- Scope: `.spw`, `docs/theory`, `prompts`.
- Ensure structural `#>` where missing (stable id, not full path slug if avoidable); layer matches folder law (grammar/semantics/pragmatics).
- Stabilize `~#` apposition names used as granule keys—**no random renames after granules land**.

**Acceptance:** parse-free apposition extract tested; particle stacks denser on scoped roots; quanta have boundary law tests.

---

### Phase 3 — Runtime field (session cache planes)

**Code**

- HotSession (or rename later): eval vs inspect planes; keys via IrRef; channel × dialect × contentHash.
- Resonance: scheduling/rank only; versioned schemes; never exact hit.
- Soft `@bc:tag` vs hard `@bc:hash` (land dirty cite/session design here if not earlier).
- Sense-cycle emits hashes/provenance; does not advance request epoch.
- Split `hot-session.ts` if still >600 when touching (eval / inspect / cite).

**Corpus (norm pass B — dialect defaults)**

- Domain `index.spw` files: `~#default_dialect` where subject is clear (b author, m registries, x process, …).
- Experimental `=exp` remains non-authority.

**Acceptance:** inspect hit ≠ eval authority; tags never in contentHash; dialect defaults documented on indexes.

---

### Phase 4 — Change reports (lex / AST) & diff legend

**Code**

- `compareLex` / `compareAst` (path-match v1) → `ChangeReport` with brace + optional topography.
- Pulse/cycle: disclose Spw change cards; layout-only ⇔ brace equal ∧ structural ops empty ∧ lex triviaOnly.
- Hunk utility from differentials; dual-read legend profile `Spw.Diff.Field` (interpretive → measured when wired).
- Split `pulse.ts` when extending (plan / apply / report).

**Corpus (norm pass C — indexes & refs)**

- Thin hubs: promote repeated cross-root paths to aliases; every CLI entry dir has `index.spw` with roots/dispatch.
- Wonder: register high-value circuits; drop empty template pairs; topic-stable ids where edited.

**Acceptance:** two revisions of a file yield lex+ast narrative; pulse can print it; hubs not sole graph gods.

**Status:** core landed (`a320db3a` — ChangeReport + `spw delta`). Pulse disclose of delta cards still open (do not grow `pulse.ts` god-file).

---

### Phase 4b — Container nest-path clusters

**Intent (original, restore):** form families from **nest paths** over the paired-boundary alphabet, not brace-count histograms alone.

**Alphabet (encode order):** `<>(){}[]`  
(plus stream `<<>>` / n-range `(())` when present — same product family, secondary).

**Focus:** **nest paths** — e.g. `{[]}` vs `[]{}` are different forms even when kind *counts* match.

**Product (landed)**

- `scanNestPaths` / `nestPathDelta` / `nestPathSpectrum` / `formatNestPathSpw`
- Unlabeled `skeleton` + `clusterKey`; labeled skeleton + labels multiset
- **Container labels on delta:** open/close labels, scope name, capsule channel/tag, simple frame param (`seed[Demo]`)
- `ChangeReport` / `spw delta`: `pathMatch` = brace equal ∧ nest skeleton equal; `layoutOnly` also requires `labelsEqual` (label rename is not layout-only)

**Doctrine note — couple vs capsule**

- Operator coupling digraph `<>` and capsule shell **were not originally meant to diverge**.
- Both share glyph `<>` in nest alphabet; kind retained for tooling. Lex reunify is a later semantics episode.

**Acceptance:** `{[]}` ≠ `[]{}` cluster keys; label rename discloses `labelDelta` and clears layoutOnly; whitespace-only still layoutOnly when nest+labels hold.

---

### Phase 4c — Patch pipeline (selection → store → apply)

**Intent:** one product spine for agent/CLI/LSP rewrite loops.

**Vocabulary:** **delta** = sense narrative (`ChangeReport`, `spw delta`); **patch** = frozen apply product (`Patch`, IrKind `patch`).

```
AstSelection  →  Delta narrative + SourceDifferential  →  Patch (IrRef)
                      ↓                                      ↓
              collate (sense)                      store: memory | ir | file
                      ↓                                      ↓
         apply target: file | files | nodes       (discharge under channel ceiling)
```

**Code (landed spine)**

- `AstSelection` — uri, span, selector (SpwPattern | citation string), nest skeleton/labels
- `Patch` — selection + `SourceDifferential` + optional `ChangeReport` + narrative + `effectCeiling` + `applyTarget` + `store`
- IrKind `patch`; schema `spw.patch/1`; product key via `irRefKey` (store/retention out)
- `buildPatch` / `buildPatchFromEdits`
- `applyPatch` (file or span/selector-scoped nodes); `applyPatchToFiles`
- `PatchMemoryBank` — session memory by irRefKey
- CLI: `spw delta --patch` (alias `--cache`) emits `^["patch"]` card (collate only; no write)

**Compose with existing**

- Semantic plans → `buildPatchFromEdits` after `planSemanticEdits`
- Range plans → same
- Nest path + labels already inside ChangeReport narrative

**Not yet (later)**

- Durable IR file pack format on disk beyond memory bank
- Multi-file patch rebased per uri (today: hash-matched apply per file)
- LSP selection → Patch round-trip
- Channel-gated apply CLI (effect group; not sense)

**Acceptance:** build → memory bank → apply round-trip with hash gate; span selection filters edits; stale beforeHash refuses; `spw delta --patch` prints dual-read card.

---

### Phase 5 — LSP index quanta & proportional invalidation

**Code**

- Extract `lsp/cache/*` (granule, validity, inbound); shrink `server-index` (no net growth).
- Fix eviction leak, recency flattening, annotations rebuild (apposition-cache-granules correctness spine).
- File fallback + apposition envelope reuse; typed products not document-bag magic.
- Request-epoch free of product keys (enforced from Phase 0).

**Corpus (norm pass D — handle-friendly labels)**

- Frame/seed names and `#>` ids stable in edited surfaces so label postings help.
- Align theory status with implemented LSP evidence where claims were overstated.

**Acceptance:** proportional invalidation tests; server-index smaller or split; index postings keyed by hash×profile.

---

### Phase 6 — Patterns, fuzzy search, CLI projection

**Code**

- Extend `SpwPattern` with optional layer, fixity, label site; PatternCapture product.
- Handle postings: pathref, label, fixity+phrase, particle (exact).
- Fuzzy: geometry bytecode + SearchTuning from dialect policy; never fake exact.
- CLI: wire find/query fuzzy; document math/materials synonym table; command groups = cost.
- Apply path: instantiate → differential → ChangeReport → channel ceiling.

**Corpus (norm pass E — pattern teaching surfaces)**

- `.spw/patterns` and key prompts: examples of dual-read, handles, collate vs discharge.
- Formula/idiom surfaces stay falsifiable.

**Acceptance:** exact revalidate from fuzzy hit; capture/apply dry-run green; CLI help states IS operation.

---

### Phase 7 — Tree hygiene land & publish

**Code**

- Migrate tests from `src/seed` / `src/runtime` into packages (H1); shim or delete legacy trees when green.
- CLI folder groups (workspace|sense|read|shape|effect|shared) as moves allow.
- Seed barrel subpaths; public index slimmed.
- Full stabilize/ship fuzz; doctor/roots/tree sanity.

**Corpus (norm pass F — walk quarantine)**

- Confirm gen/harness noise excluded from invent/map walks.
- Mounted-consumer: workbench mount still excludable.

**Acceptance:** tip green; dual-tree debt gone or shim-only; ff-push ready to `upstream/main`.

---

## Interleave diagram

```
Phase0 clocks/doctrine ──► corpus link
Phase1 IrRef/freeze ──────► alias discipline
Phase2 quanta+scans ──────► particle/apposition norm  ─┐
Phase3 runtime planes ────► dialect index defaults       │ densify statics
Phase4 change reports ────► hub/wonder/ref norm        ◄─┘ before search
Phase5 LSP granules ──────► label stability
Phase6 patterns/CLI ──────► pattern teaching
Phase7 hygiene+push ──────► gen-skip / mount check
```

Corpus passes **A–F** are not optional epilogues; each is gated with the phase that needs its statics.

---

## Files (predicted; evolve in wip)

### Doctrine / plans

```
[NEW] .agents/plans/canon-rewrite-v2/PLAN.md
[NEW] .agents/plans/canon-rewrite-v2/wip.spw
[NEW?] docs/theory/spw/operational-field.spw   # or .spw/patterns/operational-field.spw
[MOD]  .agents/plans/spw-beat-diff-precipitation/*   # absorb / supersede as needed
[MOD]  .agents/plans/apposition-cache-granules/*      # correctness spine into Phase 5
```

### Seed

```
[MOD] packages/spw-seed/src/ir/*
[MOD] packages/spw-seed/src/dialect/*
[NEW] packages/spw-seed/src/canonical/apposition-scan.ts   # or statics/
[NEW] packages/spw-seed/src/canonical/change-report.ts
[MOD] packages/spw-seed/src/query/types.ts
[MOD] packages/spw-seed/src/canonical/brace-projection.ts
[MOD] packages/spw-seed/src/canonical/topography-probe.ts
[MOD] packages/spw-seed/src/canonical/geometric-resonance.ts
[MOD] packages/spw-seed/src/index.ts
```

### Runtime

```
[MOD] packages/spw-runtime/src/session/hot-session.ts
[MOD] packages/spw-runtime/src/session/charge.ts
[MOD] packages/spw-runtime/src/session/dialect-policy.ts
[MOD] packages/spw-runtime/src/state/memory-cache.ts
[MOD] packages/spw-runtime/src/session/sense-cycle.ts
```

### LSP

```
[MOD] packages/spw-lsp/src/server-index.ts
[MOD] packages/spw-lsp/src/types.ts
[MOD] packages/spw-lsp/src/stdio-server.ts
[NEW] packages/spw-lsp/src/cache/*
[MOD] packages/spw-lsp/src/handlers/*
```

### CLI

```
[MOD] packages/spw-cli/src/pulse.ts
[MOD] packages/spw-cli/src/commands.ts
[MOD] packages/spw-cli/src/cite.ts
[MOD] packages/spw-cli/src/cycle.ts
[MOD] packages/spw-cli/src/query.ts
[MOD] packages/spw-cli/src/fs-walk.ts
[MOD?] packages/spw-cli/src/{workspace,sense,read,shape,effect,shared}/
```

### Corpus (norm)

```
[MOD] .spw/index.spw, .spw/workspace.spw, .spw/**/index.spw
[MOD] docs/theory/spw/*, prompts/** (scoped)
[MOD] .spw/conventions/cli.spw (synonym table)
```

### Legacy

```
[DEL?] src/seed, src/runtime after test migration
```

### Craft guard (must not grow without split)

- `packages/spw-cli/src/pulse.ts` (~1831)
- `packages/spw-lsp/src/handlers/display.ts` (~1461)
- `packages/spw-lsp/src/server-index.ts` (~1234)
- `packages/spw-seed/src/canonical/form-geometry.ts` (~1209)
- `packages/spw-runtime/src/session/hot-session.ts` (~763)
- `packages/spw-seed/src/index.ts` (~764)

---

## Discharge units (illustrative, not a quota)

Use sigils when discharging; order follows phases. **Skip, merge, or split freely.**

```
.[doctrine] — operational field coordinates and collate/discharge law
vocab[clocks] — request epoch vs session beat; brand types
&[seed-ir] — IrRef product identity and law tests
^seed[quanta] — token windows, apposition scan, boundary contracts
.[corpus] — particle stacks and apposition stability (.spw/theory/prompts)
vocab[fixity] — stamp actPlacement; phrase census static
&[runtime] — eval/inspect planes; resonance scheduling only; @bc hash vs tag
.[corpus] — domain index dialect defaults
^seed[change] — LexDelta/AstDelta/ChangeReport
&[cli] — pulse/cycle structural disclose; diff legend profile
.[corpus] — hub thinning, aliases, wonder registries
![lsp] — pin cache defects; extract cache modules; granules
.[corpus] — label/#> stability for handles
^seed[pattern] — layer/fixity/label on SpwPattern; handle postings
&[cli-lsp] — fuzzy search tuning; exact revalidation
.[corpus] — pattern teaching surfaces
&[hygiene] — tests into packages; CLI folders; barrel subpaths
![ship] — fuzz stabilize/ship; push ff upstream
```

---

## Agentic Hygiene

- **Publish base:** `upstream/main@d9da60ad0bbfd90ebaa5742709f162b61f041bae`
- **Capability reference (disposable history):** tip `8bb423c7` and/or tree of local `main` ahead 31—not required as commit ancestry
- **Mid-rebase:** abort `wt-canon-rewrite-v2` rebase; do not continue stop-by-stop rewrite
- **Dirty isolation:** cite/hot-session WIP → park branch before rebuild; re-land in Phase 3/6
- **Hygiene split:** if main accumulates unrelated work during rebuild, keep it off the rewrite branch
- **Rebase cadence:** rebuild branch stays based on upstream; avoid long divergence from tip backup
- **Privacy:** no absolute user paths in commits

---

## Dependencies

| Plan / surface | Relation |
|----------------|----------|
| `spw-beat-diff-precipitation` | Supersede clock/activity/cache *claims*; absorb review receipts as doctrine |
| `apposition-cache-granules` | Phase 5 correctness spine (eviction, granules, inbound) |
| `geometric-analysis-tooling` | shapeFingerprint as later granule key—not Phase 2 blocker |
| `cli-sense-reorientation` | Naming already partially landed; extend synonym table |
| v1 `feature/canon-retrospective-rewrite` | Non-goal; archive after backup |

---

## Failure Modes

| Class | Mode | Mitigation |
|-------|------|------------|
| Hard | Tip import loses capability | Backup branch; capability checklist vs tip tree |
| Hard | Corpus renames break refs | Alias promotion first; plan-only ref maps; pulse/topo |
| Hard | Apposition granules regress majority without appositions | File-quantum fallback must match today’s speed |
| Soft | Fuzzy treated as exact by agents | Always score + revalidate path |
| Soft | God-files grow during phases | Freeze rule; split on touch |
| Soft | Dual `src/` / `packages/` drift | H0 no new src; H1 migrate tests |
| Non-negotiable | Product key includes request epoch / tier / resonance | Type + tests |
| Non-negotiable | Silent discharge (write without channel/plan) | CLI groups + effect ceiling |
| Non-negotiable | Layout-only write when structure moved | brace + ChangeReport |

---

## Verification

Per phase: scoped tests (seed/runtime/lsp/cli as touched) + `fuzz:types` when types move.

Stabilize before push: `fuzz:stabilize` / `fuzz:ship` as appropriate.

Corpus: `lint:spw` / `spw tree` / `spw census` / `spw graph` on scoped roots; gen skipped in walks.

Manual checklist: capability parity with tip for cite/follow, measure, pulse plan, dialect surface card, navigation smoke.

---

## Success criteria (program complete)

1. History from upstream is **short or arbitrary** but **lawful**—not a museum of temperature/beat conflation.
2. Field coordinates documented and enforced in keys and brands.
3. Corpus scoped roots denser in particles, indexes, aliases; wonders registered not only footered.
4. Change can be told as lex + AST + brace, not only bytes or `structureMoved`.
5. Patterns searchable exact+fuzzy with handles; apply is discharge-aware.
6. JS tree: packages primary, god-files shrinking, CLI cost groups visible.
7. `upstream/main` updated by fast-forward when human authorizes push.

---

## Relationship to conversation artifacts

This plan consolidates design from the rewrite-v2 discussion: disposable unpushed history, operational physics, quanta/cache, corpus norm, JS hygiene, TS/CLI/info-sci, diff/hunk/operator legend, lex/AST change reports, pattern capture and fuzzy index handles. Prefer this slug over continuing mid-rebase archaeology or a fixed 4–7 commit theater.
