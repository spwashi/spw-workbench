# Cognitive Laser Pointer — Differential Block Projection
# theory.spw.pointer

#:layer #!semantics

// The Cognitive Laser Pointer is a differential interface metaphor where user focus (gaze/caret) acts as a projective query against the system.

// Instead of passively viewing state, user attention projects a Spw Block onto the environment, altering interpretation and feedback.

## 1. The Differential Signal

The system tracks the **differential** between the user's current context and the system's resting state. This differential is encoded as a **Block Projection**.

```spw
Projection = Lens(Focus, Selection) -> Block
```

// Focus: the active UI region or semantic node (for example editor, inspector, or sidebar).
// Selection: the specific content held in focus (for example a text range or AST node).

## 2. Projection Atlas

The **Atlas** maps regions to Spw Operators, defining the semantic "flavor" of the projection.

// Editor — Action (!): kinetic energy, creation, mutation.
// Inspector — Wonder (?): investigation, query, debug.
// Sidebar — Structure (%): architecture, hierarchy, organization.
// Context — Perspective (@): point of view, lens, filter.

## 3. Block Structure

A projection is serialized as a Spw Block:

### Granular Focus (No Selection)
Prefix syntax indicating "working within" a structure.
```spw
%["sidebar"] // Operating within the sidebar structure
```

### Specific Selection (Has Selection)
Postfix syntax indicating "operating upon" a subject.
```spw
editor!["function main()..."] // Acting upon the specific code function
```

## 4. Shader Resonance

The projection drives the **Shader Pipeline** by injecting semantic saturation values (`u_material`).

// Injection (! Action): high-saturation red/orange.
// Probe (? Wonder): high-saturation cyan/blue.
// Ground (. Signal): low-saturation ambient green.
// Structure (% Matrix): high-contrast ordered patterns.

This feedback loop ensures the visual environment *feels* like the cognitive mode the user is inhabiting.

#>wonder_docs_theory_spw_cognitivelaser_1
?["What happens when you zoom from the concept level to the workspace level in Cognitive Laser Pointer — Differential Block Projection?"]{
  #:depth #!mathematical // lens: ecological zone
  $^["_"]@here => &[@workspace] // local → global projection
  ~#zoom_in: "cognitive laser pointer — differential block projection" // this file's atom
  !probe{ "the atom is concept-scale; zoom out: what workspace-level pattern does this concept participate in?" }
  $%[runtime.stage, runtime.queue_depth]
}

#>wonder_docs_theory_spw_cognitivelaser_2
?["What ripples if #cognitive_laser changes in this file?"]{
  #:depth #!stylistic // lens: molecular binding
  $#educational@workspace => %[reference.count, depth.downstream]
  ~#downstream: "trace all files that reference this surface"
  !probe{ "imagine renaming #educational to #educational_v2; count broken references; the count reveals how deeply s current context and the system is woven into the workspace." }
  $%[runtime.stage, runtime.queue_depth]
  ~<"docs/theory/spw/audio-architecture.spw"> ~#neighbor(nearest)
}
