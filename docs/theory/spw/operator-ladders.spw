// Operator Ladders (secondary)
#
// Operator compositions with paired-boundary form contours.
// Canonical home: form-ladders.spw.
#
// @see docs/theory/spw/form-ladders.spw
// @see packages/spw-seed/src/canonical/form-ladders.ts

#:layer #!semantics

^seed[Spw.Operator.Ladders v:0.3 @profile:interpretive @intent:operator_boundary_composition]

^["redirect"]{
 primary: ~"./form-ladders.spw"
 rule: `The catalog records observed or proposed boundary products; preferredBoundary is not a universal evaluation law.`
 set: `Paired-boundary kinds are disclosed by the active Form Ladder profile; local brace vocabularies may exclude angle forms.`
}

^["note"]{
 dropped: `_cap / capacitance as canonical exemplar — use hole _, void empty forms, ground, fold`
 confluence_example: `& => {&} => &[a, b] => {&[describe!]} => &(a, b, c) => .{&: _}`
 coupling_example: `<> => <>["a", "b"] keeps kind=couple while composing with a Frame operand surface.`
}
