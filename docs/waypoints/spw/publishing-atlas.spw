# publishing/ATLAS — Publishing Domain Orientation
#
# After commits 23–25. Publishing exists as a domain.

#:layer #!pragmatics

^["genre system"]{
  @publishing/genre/presets.ts   — ~"8-dimensional genre coordinates"
  @publishing/genre/manager.ts   — ~"genre selection + CSS variable application"
  @publishing/genre/types.ts     — ~"GenreDefinition with intent, medium, mode"

  ^["eight axes"]{
    .. LoD, Disclosure, Context, Lens
    .. Valence, Pacing, Intent, Medium
  }
}

^["prism universe"]{
  @publishing/prism/manager.ts   — ~"multi-facet personality blending"
  @publishing/prism/types.ts     — ~"ecology: focused 60%, unfocused share 40%"

  ~#ecology: "each facet is a colored lens; the prism combines them"
}

^["authoring surface"]{
  @app/components/authoring-editor/authoring-editor.ts  — ~"Spw-native editing with syntax hl"
  @app/components/publish-dialog/publish-dialog.ts       — ~"publication pipeline UI"

  ^["editing features"]{
    .. debouncing, autosave, streak tracking
    .. canonical operator names in editor mode
    .. XSS prevention, type safety, resource cleanup
  }
}

^["what changed"]{
  23-runtime     — ~"quantum metaphor + bytecode benchmarks + modular dispatch"
  24-publishing  — ~"genre system + prism universe + RAF batching"
  25-authoring   — ~"Spw-native editor + publication library + localStorage"
}

?["How does this surface orient the developer's attention or workflow?"]
  #:scope #!file
  ~#probe: "trace which claims or conventions reference this surface"

