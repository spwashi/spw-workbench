# Agent Loop
#
# Reversible loop for agent-assisted edits and SVG/motion output.

#:layer #!semantics

^["goal"]{
 ~#intent: "Agent output becomes structure, not just text."
 ~#contract: "Every patch is previewable, stageable, and replayable."
}

^["loop"]{
 select: "user selects span/node/context"
 intent: "generate SVG / refactor / explain / add motion spec"
 respond: "agent returns patch + explanation + checks + preview artifacts"
 decide: "apply, stage, replay reasoning"
}

^["state_preservation"]{
 ~#requirements{
 selection_anchor: "preserve by token/node id"
 ast_remap: "map highlights by identity"
 snapshots: "before/after with delta"
 }
}

^["outputs"]{
 ~#artifacts{
 patch: "diff or changeset"
 explanation: "reasoning summary"
 checks: "lint/a11y/perf"
 previews: "SVG/motion tokens"
 }
}

#>wonder_docs_runtime_spw_agentloop_1
?["What concept is conspicuously absent from Agent Loop?"]{
  #:depth #!stylistic // lens: molecular binding
  ~#present: [#educational, #mathematical]
  ~#expected_but_missing: "?" // the reader fills this gap
  !probe{ "list what IS annotated; list what sibling files annotate; the difference is this file's blind spot or intentional boundary." }
  $%[file.frame_count, file.annotation_density, file.brace_depth_max]
}

#>wonder_docs_runtime_spw_agentloop_2
?["How does goal teach loop?"]{
  #:depth #!educational // lens: ecological zone
  $^["goal"] => $^["loop"] // trace the edge
  &[@_, @spw] // confluence of references
  !probe{ "draw the relationship: is it containment, reference, or emergent? does reversing the direction change meaning?" }
  $%[file.frame_count, file.annotation_density, file.brace_depth_max]
  ~<docs/runtime/spw/brace-registers.spw>  // nearest neighbor
}
