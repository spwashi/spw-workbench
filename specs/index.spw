# Specifications Index
#
# Normative contracts, strict policies, and formal alignment.

^["roots"]{
  @here: ~"."
}

^["contracts"]{
  !["Binding Contracts"]
  .. ~<contract>"./classname-contract.md"         # CSS classname contract
  .. ~<contract>"./ui-state-contract.md"          # UI state surface contract
  .. ~<policy>"./shadow-dom-policy.md"            # Shadow DOM policy (markdown rail)
  .. ~<policy>"./shadow-dom-policy.spw"           # Shadow DOM policy (spw index)
}

^["alignment"]{
  !["Alignment & Normalization"]
  .. ~<spec>"./spec-alignment.spw"                # Gap analysis vs spec
  .. ~<spec>"./reference-conventions.spw"         # Reference conventions
  .. ~<spec>"./data-attribute-normalization.spw"   # Perspective/LoD attributes
  .. ~<summary>"./formalization-phase-1-summary.md" # Phase 1 formalization summary
}

^["validation"]{
  !["Validation"]
  .. ~<audit>"./audit-classnames.md"              # Classname validation audit
}
