# Runtime Index
#
# Execution engine, performance optimization, protocols, and state management.

^["roots"]{
  @here: ~"."
}

^["engine"]{
  !["Execution Models"]
  .. ~<runtime>"./agent-loop.spw"                 # Reversible agent loop
  .. ~<registers>"./brace-registers.spw"          # Brace dyads for autocomplete
  .. ~<hud>"./hud-runtime.spw"                    # Session/phase/feature HUD
  .. ~<registers>"./register-bank.spw"            # Operator-typed register bank
}

^["performance"]{
  !["Optimization & Web APIs"]
  .. ~<perf>"./optimization-plan.md"              # Performance optimization plan
  .. ~<lsp>"./lsp-dap-workers.spw"                # Worker protocol prep for LSP/DAP
  .. ~<web>"./web-apis.spw"                       # Browser API opportunities
}
