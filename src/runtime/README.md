# Runtime Foundation (v0.2.0-alpha)

This folder contains the runtime foundation for the canon rewrite.

Scope for this release slice:
- deterministic register state container
- minimal ONF-backed interpreter stub
- parse-to-runtime pipeline bridge

Naming policy:
- runtime filenames use kebab-case
- one concern per file
- no UI/platform imports from runtime core
